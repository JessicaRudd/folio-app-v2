version: '3.8'

services:
  web:
    image: us-central1-docker.pkg.dev/folio-app-492702/folio-repo/web:latest
    ports:
      - "3000:3000"
    environment:
      - FIREBASE_API_KEY=${FIREBASE_API_KEY}
      - MUSIC_KIT_TOKEN=${MUSIC_KIT_TOKEN}
      - RESEND_API_KEY=${RESEND_API_KEY}
      - RESEND_FROM_EMAIL=${RESEND_FROM_EMAIL}
      - RESEND_BRAND_URL=${RESEND_BRAND_URL}
    # GCP Cloud Run Extensions
    x-google-cloud-run:
      min-instances: 0
      execution-environment: gen2

  image-proc:
    image: us-central1-docker.pkg.dev/folio-app-492702/folio-repo/imgproxy:latest
    ports:
      - "8080:8080"
    environment:
      - IMGPROXY_LISTEN=:8080
      - IMGPROXY_MAX_SRC_RESOLUTION=50
